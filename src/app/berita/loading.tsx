import Loader from "@/components/Loader";

export default function Loading() {
  return (
    <Loader
      label="Memuat berita"
      hint="Mengambil artikel dan foto kegiatan terbaru."
    />
  );
}
